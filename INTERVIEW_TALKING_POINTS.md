# ops-assistant 面试讲解词（TALKING_POINTS）

> 给面试用的「怎么讲这个项目」。避免逐行背代码，讲**定位、架构、决策、量化成果、取舍**。

## 一句话定位
> 基于 openclow 平台的**智能运维排障 Agent**：用户报一个故障现象，它会自己调用工具（探活/日志/资源/服务状态/排障手册检索）收集证据，再给出**结构化、可执行**的排障诊断报告——而不是背文档。

## 架构（分层，讲清楚边界）
- **openclow**（底座，AI 能力层）：LLM、嵌入、RAG 检索、鉴权、监控等通用能力。
- **ops-assistant**（业务应用层）：聊天界面、会话持久化、Query 改写、**Agent 工具循环**、诊断报告、工具审计、评测。
- 业务层只通过 API 调平台，不侵入平台源码 → 体现「平台 + 业务应用解耦」，可独立迭代/部署，未来多业务共享一个底座。

## 核心功能（挑重点讲）
1. **Agentic 工具排障**：不是“检索→复述”，而是“**收集证据→下结论**”。
   - 工具：`probe_http` / `query_logs` / `check_resources` / `service_status` / `search_runbook`(RAG)。
   - 工具**双后端**：`sim`(固定样本，可复现/可测试) / `real`(打真实服务)。
2. **结构化诊断报告**：症状 / 证据 / 根因 / 严重度 / 处置步骤 / 置信度 / 来源，`DiagnosisReport` 强类型。
3. **多轮 Query 改写**：把「那怎么修复？」这类追问，改写为独立问题再检索，形成「改写→检索→答」闭环，前端可见「🔍 改写后检索：…」。
4. **SSE 流式**：工具调用轨迹、最终报告逐段推送，不是一次性。
5. **会话持久化**：SQLite + 前端 localStorage，刷新/重开不丢；支持逐条删除（❌）和一键清空（按用户隔离）。
6. **多用户 + 凭证鉴权**：身份由 `X-API-Key` 决定（`OPS_ASSISTANT_USERS` 映射），不再信任客户端自报 `X-User-Id`；会话/审计按用户隔离。
7. **工具审计**：每次工具调用记录（谁/何时/参数/结果/耗时），可观测/合规。
8. **健壮性**：LLM 失败有限重试、证据截断、优雅降级（平台不可用给友好提示）、硬拦截重复工具调用。
9. **安全**：.env 已 gitignore、密钥占位符、CI 加 gitleaks、破坏性请求拒答。
10. **RAG-first 只答知识库**：每次提问必先查真实知识库（47+ 文档），只基于检索结果回答；检索不到就诚实说「知识库中无相关信息」，不编造（grounded，不幻觉）。
11. **自我成长（learning loop）**：agent 答不上时记录「未覆盖问题」；你解决后一键沉淀成 runbook 灌入知识库 → 下次可答。**已在真实环境验证「未覆盖 → 沉淀 → 再问可答」闭环。**（半自动：agent 发现不会的，你确认后教会它，避免把错误知识灌进去。）
12. **真实业务指标（可落地）**：不只是"能回答"，还能量化。
    - **用户反馈闭环**：回答下方 👍/👎 + 是否解决 + 用时 → `feedback` 表 → 解决率/满意率/平均解决耗时。
    - **真实巡检/incident**：后台健康监控线程定时探真实靶点，异常产生 incident，agent 自动诊断 → 发现数 / MTTR / 诊断成功率。
    - 都是 `GET /api/metrics` 聚合，前端「指标」面板即看。**把系统从 demo 变成有度量的系统。**

## 评测与工程质量（量化，能讲数据）
- **Agent 四类指标（实测 `--live`）**：工具序列正确率 **80%**、结论正确率 **100%**、拒答正确率 **100%**；检索 Recall@1/@3/@5 = **90%/100%/100%**、**MRR=1.0**。（mock 版用于 CI 离线校验编排管线，数值恒定；`--live` 为真实模型+真实 RAG 实测一次。）
- **真实 RAG 召回**（灌库后 live）：Recall@1≈90%、Recall@3/5=100%、**MRR=1.0**（相关手册几乎总是排名第 1）。
- **测试 + CI**：`pytest` 全绿（本机跑）+ GitHub Actions（pytest + 两个 mock 评测 + gitleaks）。
- **真实联调**：`103.236.98.200` 上 openclow 平台实测，agent 在真实 LLM 驱动下自主选工具并给出正确诊断。
- **业务指标维度（真实数据）**：解决率 / 满意率 / 平均解决耗时（用户反馈闭环）+ 巡检发现数 / **MTTR** / 自动诊断成功率（真实巡检 incident）。这些都是 `GET /api/metrics` 聚合、前端可见，证明系统"可落地、可度量"，而不是只能跑 demo。

## 关键设计决策 + 取舍（这些最加分）
- **为什么 agent 工具循环放业务层、不放平台**：平台保持通用；业务自己掌控排障逻辑与评测，体现解耦。
- **为什么默认 `json` 工具协议、`native` 可切**：`json` 兼容现有 `/chat/raw` 无需改平台即可跑；`native`(原生 function-calling) 是平台升级后的可选增强——讲了「兼容 vs 演进」。
- **为什么最终回答用自然语言而非严格 JSON**：模型产出含引号/内嵌 JSON 的字符串，严格 JSON 容易解析失败；让「工具调用=JSON(短、无文本)、最终回答=自然语言」，**保证永不把原始 JSON 给用户**。
- **为什么 `sim/real` 双后端**：`sim` 可复现/可测试/无外部依赖（CI 友好、演示稳定）；`real` 对真实服务真排障。两种都验证逻辑。
- **为什么硬拦截重复 `search_runbook`**：模型会在越界/复杂问题上反复检索，硬拦截保证收敛、少一次无效调用。
- **为什么凭证决定身份**：多租户安全的正确姿势——不能信任客户端自报的 user。

## 面试演示流程（2 分钟走一遍，照 TEST_GUIDE）
1. 问：`服务 /health 返回 503，日志报 Invalid API key` → 看工具逐步流出 → 结构化诊断。
2. 追问：`那怎么修复？` → 看「🔍 改写后检索」+ 结合前文的修复指引。
3. 越界：`帮我直接删掉数据库所有表` → 看拒答 + 安全替代。
4. 多用户：切 `alice`/`bob` → 看历史互不可见。
5. 补充：`curl /api/audit` 看审计；`python -m eval.run_agent_eval` 看评测。

## 已知边界/不足（诚实，别被人问倒）
- **真实检索召回依赖已灌库**；`real` 模式的日志/资源/服务状态在非服务端主机会降级为样本。
- **多用户鉴权**用 API Key+用户映射；未接 OAuth/JWT 细粒度授权（SSO/审计权限分级可扩展）。
- **评测集规模有限**（5 个 agent 用例 + 5 个检索用例）；可扩到更多真实 runbook/案例。
- **会话隔离是单库**（SQLite），生产多实例可换 Redis/PostgreSQL。
- **浏览器关标签不退出服务**，需关启动器窗口或重启清理（已做启动前清理旧进程）。
- **业务指标"机制就绪、数字待喂"**：反馈闭环与巡检/指标都已实现、口径已定义；但「解决率/MTTR」的真实数字需要真实用户/真实故障数据喂入（`--live` 连真实平台可出），指标页本身不会造假。

## 可能被追问 + 怎么答
- **“你这跟普通 RAG 问答有什么区别？”**：RAG 只检索+复述；这是 Agent 会**调用工具拿真实证据**，再基于证据给结论，且可动态决定查哪些。
- **“怎么证明它真的解决了问题？”**：真实 `--live` 评测——结论正确率 100%、拒答 100%、工具序列 80%、检索 Recall@1=90% / MRR=1.0（相关手册基本总能命中且排第 1），且 demo 里针对不同故障给出不同诊断；再加上**用户反馈闭环**（👍/👎+是否解决）与**真实巡检 incident**（agent 自动诊断）→ `GET /api/metrics` 直接给出解决率 / 满意率 / MTTR / 诊断率这类**经营指标**。
- **“有没有真实用户/真实指标？”**：机制已就绪——反馈采集 + 自动巡检 + 指标聚合/展示都已落地、口径已定义；真实数字需要真实流量喂入（`--live` 连真实平台可出）。我把它做成"数据一进来就是指标"，而不是编一个假 99%。
- **“为什么不用现成 LangChain Agent？”**：openclow 已有平台能力，我把**业务编排**独立出来自己掌控（解耦），并可按需复用平台的原生 function-calling。
- **“评测会不会过拟合？”**：mock 是校验编排管线；`--live` 用真实 LLM + 真实 RAG 出数字，且含越界拒答用例。
